# Build plans — index

Dependency-ordered execution plans, one per phase (spec §13 is the roadmap; each phase gets its plan at phase start). Review reports and resolution maps for each plan live in [docs/reviews/](docs/reviews/).

| Phase | Plan | Status |
|---|---|---|
| 0 — Foundations (workspace, money core, schema v1, converse skeleton) | [docs/plans/phase0-foundations.md](docs/plans/phase0-foundations.md) | ✅ Built & exit-checked green (2026-08-12): 54 Rust tests, 100% line coverage on `domain` (978/978), 22 Python tests, clean-clone verified |
| 1 — Core write path (application ports + use cases, SQLx/Axum adapters, main wiring, text-to-trade e2e) | [docs/plans/phase1-core-write-path.md](docs/plans/phase1-core-write-path.md) | ✅ Built & exit-checked green (2026-08-12): 152 Rust tests, 33 Python tests, line coverage `domain` 100% (978/978), `application` 98.35% (3,097/3,149), `adapters` 92.72% (891/961), clean-clone + fresh-DB + text-to-trade e2e verified |
| 2 — Core loop UX + oracle defense (outbox→WS gateway, lifecycle scheduler + auto-resolve, D21 integrity bar in CastVote, charts/tape, installable PWA) | [docs/plans/phase2-core-loop.md](docs/plans/phase2-core-loop.md) | ✅ Built & exit-checked green (2026-08-12): 214 Rust tests, 33 Python tests, 21 web vitest, line coverage `domain` 100% (978/978), `application` 100% (4,594/4,594), `adapters` 100% (1,849/1,849), live-loop e2e (`snapshot→price+trade→closing→closed→resolved`, Closing→423, payout≤2s, escrow=0) |
| 3 — Economy & full integrity (rep EWMA + tier gates/fee discounts, leaderboards, resolution integrity sweep + payout hold, sybil signal capture, LP PnL kill-switch, fee summary) | [docs/plans/phase3-economy-integrity.md](docs/plans/phase3-economy-integrity.md) | ✅ Built & exit-checked green (2026-08-12): 255 Rust tests, 33 Python, 27 web vitest; line coverage 100% `domain` (1,326), `application` (6,639), `adapters` (2,491); `scripts/e2e_economy.sh` PHASE 3 E2E GREEN (A rep=8516+avg board, B hold→pass, C flag+curator void, D fee 90bp buy / base sell flip) |
| 4 — Social & notifications (threaded comments + votes + deterministic moderation/reports, Top Holders, public profiles, §7.1 notification fanout via a cursor-tracked second outbox consumer, WS user channel) | [docs/plans/phase4-social-notifications.md](docs/plans/phase4-social-notifications.md) | ✅ Built & exit-checked green (2026-08-12): 301 Rust tests, 33 Python, 40 web vitest; line coverage 100% `domain` (1,502), `application` (8,858), `adapters` (4,091); `scripts/e2e_social.sh` PHASE 4 E2E GREEN (reply/mention collapse+WS, hot vote flip, brigade floor+restore epoch, resolution_trade+score, mention cap, mark-read, as_of cursor) |
| 5 — Content engine (draft→curate→scheduled publication, video/poster job engine with hot-swap, share cards, env-gated LLM draft/moderation adapters; **4-way parallel build wave** enabled by the module split) | [docs/plans/phase5-content-engine.md](docs/plans/phase5-content-engine.md) | ✅ Built & exit-checked green (2026-08-12): 455 Rust tests, 29 Python passed + 4 skipped, 50 web vitest; line coverage 100% `domain` (1,897), `application` (13,368), `adapters` (6,672); frozen manifest + deterministic OpenAPI/model generation verified; `scripts/e2e_content.sh` PHASE 5 E2E GREEN (curation saga, asset WS + rehydration, vote/trade/settle/share, cadence/idempotency/concurrent leasing, honest LLM fallback) |
| 6 — simswarm to 2,000 agents + chaos + ops control plane | [docs/plans/phase6-simswarm-ops.md](docs/plans/phase6-simswarm-ops.md) | ✅ Built & exit-checked green (2026-08-13): 696 Rust, 36 Python, and 58 web tests; line coverage 100% `domain` (1,897), `application` (21,497), `adapters` (8,826), and `simswarm` lib (2,990); deterministic OpenAPI/model generation and frozen manifest verified; `scripts/e2e_swarm_smoke.sh` PHASE 6 E2E GREEN (convoy 33/56 = 58.9%, lifecycle signals 1, fat-pot signals 3, payout transactions 1, config/switch/crash/RBAC/unwind/remedial/receivable proofs, thin mobile Playwright) |
| 7 — Money hardening + compliance gate → beta (withdrawal pipeline with hold-first ledger legs + exactly-once send lineage, deposit observation/admission machine with source-locked refunds, bonus credits with fees-paid-at-Paid conversion + segregated reserve, KYC/sanctions/geo/AML + responsible gaming, SLO gates + durable alerting, live fee override; **4-way parallel build wave**) | [docs/plans/phase7-money-compliance.md](docs/plans/phase7-money-compliance.md) | ✅ Built & integrated (2026-08-14): plan review converged after **5 codex + 4 grok adversarial rounds** (~70 findings, resolutions in `docs/reviews/p7r{1,2,3}-resolution.md`, double-APPROVED at revision 3.3); wave W1–W4 complete + coordinator integration (routes/OpenAPI mount, rails startup validation, sandbox screening, durable alert pump, 0011 lineage indexes + rail fingerprint); **1,027 tests across 23 binaries green**, clippy `-D warnings` clean workspace-wide, fmt/deps-check/frozen-manifest green, and **100% line coverage with zero exclusions** on `domain` (1,934), `application` (38,492), `adapters` (14,111) and `simswarm` lib — now gated on the lcov **union** via `scripts/coverage_gate.py`, because llvm-cov's summary max-folds instrumentation records and under-reports. Verification also caught three defects the wave's own green reports had hidden: three Pg contract suites were silently skipping (each required its own wave DB in `DATABASE_URL`, so 26 tests "passed" in 0.00s without a database — now each creates and migrates its own scratch DB, which their `TRUNCATE ... RESTART IDENTITY` demands), and `alert_outbox.last_paged_at` existed only in one worker's private database and was missing from 0011 |
| 8 — Learning site for non-engineers | [mkdocs.yml](mkdocs.yml) + [docs-site/](docs-site/index.md) | ✅ Shipped (2026-08-14): 26-page plain-language MkDocs guide (~36k words, 28 mermaid diagrams, material theme) covering the product, scoring and reputation, the architecture, the eleven migrations, live updates, the control plane, the money rules, the fifteen withdrawal states, step-by-step walkthroughs, local setup, the swarm, and the build story. Audited page-by-page against the source: fee/pricing mechanics, the nine account classes, the fourteen invariant identities, the W1–W15 withdrawal table and the real compliance rules were all corrected against `crates/` and `migrations/`; every cited path and internal link is verified and `mkdocs build --strict` is clean with link and anchor validation enabled; `just docs` / `just docs-build` |

**Engineering bar for every phase** (spec §5.5, mechanically enforced): clean architecture with the dependency rule checked in CI (`scripts/check_dependency_rule.py`), SOLID via ports/contract-test suites, named patterns only where they solve a real problem (outbox, saga/idempotency, repository+UoW, typestate, strategy, newtypes), coverage gates (`domain` 100% line; `application`/`adapters` ≥90% ratcheted; mutation kill ≥90% on the money core).
