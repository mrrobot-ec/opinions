# Phase 7 review round 1 — coordinator resolutions

Inputs: `codex-p7r1.md` (FIX-FIRST: 6B/10M/3m), `grok-p7r1.md` (fix-first: 6B/6M/6m + client-input checklist). Every finding is dispositioned below; plan revision 2 carries all accepted resolutions. Convergent findings are resolved once.

## Blockers

| Finding | Disposition |
|---|---|
| codex B1 + grok B2 (credit conversion illegal under per-currency ledger; identity restatement false; collateral of credit-funded shares undefined) | **ACCEPTED — design pivot.** Phase 7 credits are **not spendable on trades**. Grant = `House(UsdcCredit) → User(UsdcCredit)` from a pre-funded, capped house credit pool. Convert = **two ledger txns in one DB tx**: `User(UsdcCredit) → House(UsdcCredit)` retire + `House(Usdc) → User(Usdc)` pay from a pre-funded bonus reserve; cash `External` never touched. Reserve-coverage invariant added (outstanding convertible credits ≤ bonus reserve). D31's "identity 1" restatement withdrawn — Withheld and DepositSuspense are new non-external `OwnerType`s that fall into the existing identity 3 automatically (codex m1 folded in). Manual grants mint UsdcCredit, never cash. |
| grok B1 (wager-through + referral = self-serve mint) | **ACCEPTED in full.** Conversion trigger changed to **fees-paid**: a grant lot converts when the user's cumulative cash trade fees (to `OwnerRef::Fees`) attributed since the lot's grant ≥ the lot amount — house is made whole before any credit becomes cash; wash round-trips now *cost* the attacker exactly what conversion requires. Grant lots (codex M5) with per-lot consumed-fee allocation, unique trade-id contribution. Referral: server-issued codes; binds on verified phone (D21 OTP is now **in scope** for grant issuance), device HMAC, first funding instrument, dest wallet; referrer≠referee on every bind key; both legs only when the referee's first **Paid** market has buy-notional ≥ pinned floor; daily + house mint caps at remedial grade. E2E legs that must go **red** on the farm (wash-after-grant no-convert, A↔B once, third-device zero-grant). simswarm gains `ReferralChain` + `BonusWash` rings (grok m6). |
| codex B2 (memo+CAS is not exactly-once send) | **ACCEPTED.** New durable `withdrawal_send_attempts` authority: leased claim under row lock; construct and persist immutable signed tx bytes + signature **before** broadcast; only ever rebroadcast those bytes while landing is `unknown`; blockhash-expiry replacement requires proof of non-landing at expired blockhash; receipts persisted with uniqueness; `unknown` keeps funds Withheld and pages; `failed` only on definitive non-landing. Full crash/concurrency test matrix as listed. |
| codex B3 + grok B3 (lien is lock-free read; withdraw not in lock graph; dest trusted; unwind race) | **ACCEPTED.** Withdrawal request tx protocol pinned: remote decisions first (no locks) → `serialize_key` (fingerprint on hit: user, amount, canonical dest, policy fields → original receipt or 409) → **`lock_user`** (same class-2 lock as PlaceTrade; user before any money lock, never market locks) → revalidate status/KYC/config/geo/sanctions freshness under lock → **auto-collect `min(cash, Σ open receivables)`** → available check → limit reserve → hold remainder → insert row+events → commit. Lien **rechecked at send claim**. Unwind meeting an unsent withdrawal: cancel + release-then-collect **in the unwind tx**; sent-but-unsettled funds are already outside the lien and the shortfall opens a receivable. Cash-creating paths (convert, payout, sell, remedial) invoke the shared collection routine. Dest: pubkey-validated, confirm-echo, bound novelty risk reasons (first-time dest, dest shared by ≥K users) never auto-approve; per-user AND per-dest AND global hot-wallet daily caps; user pays network cost, min ≥ dust+gas (grok B3.6, codex M8 fee split). `CreditDeposit` gains `lock_user` (grok m1). |
| codex B4 + grok B4 (fail-closed gate vs money that arrives anyway; compliance ordering) | **ACCEPTED.** Observation/availability split: every finalized inflow at depth books `External → DepositSuspense` (new non-negative internal class) + immutable observation fact, always. Admission (KYC tier ≥ config, geo Clear, sanctions Clear, `pause_deposits` off, user deposit limit) moves `DepositSuspense → User` in its own audited tx; refusal leaves suspense + pages compliance; refund = the withdrawal-grade send protocol to source. Geo captured at authenticated initiation via the existing `TRUSTED_PROXY_CIDRS` rule; converse passes the user's KYC-derived region, not the box IP. KYC/geo/sanctions enforced fail-closed on signup-grant, deposit admission, PlaceTrade, **CastVote**, withdraw request. Webhooks: signature-verified, user resolved from our records, durable authenticated inbox (codex M2 algebra: same key+hash → original result; same key+different hash → typed conflict + page). Sanctions also at deposit admission and first trade; hit = dual-control compliance hold. Sandbox KYC-to-full route is staging-two-factor gated like the faucet. |
| codex B5 + grok M2 (withdrawal authority can't prove 1:1; 0011 CREATE collides with 0001) | **ACCEPTED.** `0011` is **ALTER** on the existing `withdrawals` (grok caught the collision): keep `queued|risk_hold|sent|settled`, add `denied|failed`; add `hold_tx_id` (NOT NULL post-queue), `release_tx_id` XOR `settle_tx_id` state checks, request fingerprint, approval proposal/confirmation rows, send-attempt + receipt linkage; every transition CAS'd and coupled in-tx to its ledger txn + audit + outbox. New invariant identities (a) `balance(Withheld) = Σ` active unsent/sent-unsettled amounts, (b) one exact hold per row, (c) exactly one terminal inverse XOR settlement leg, (d) outgoing payment facts/signatures 1:1 with withdrawals, (e) no duplicate terminal effect — fake+Pg+chaos, not happy-path only. `users.kyc_tier` stays `int` (0/1/2 mapping documented). Withheld/DepositSuspense = `OwnerType` ALTERs. |
| codex B6 (ownership matrix not file-level) | **ACCEPTED.** §2 rewritten as an exhaustive path-level matrix; 7.0a owns every shared skeleton surface (domain vocabulary, ports/exports/factories, error/model, route+OpenAPI registration, placeholders, main slots, catalog+validator, dep rules, scripts, manifest); `place_trade.rs` + trade tx adapters get a **single owner** (W3) who integrates W2/W4 needs; deposit path single-owned (W3); routes/e2e/web sections owned per file; frozen ask protocol restated. |
| grok B5 + codex M9 (SLO gate vacuous; release gate regressed to smoke) | **ACCEPTED.** Targets are **constants in the simswarm lib** (300ms/1s/100ms per spec §9); `--slo-gate` only flips enforce; per-series `n_min` — empty/thin series **fails**; runner records `close-to-paid` and `ws-delivery` live; timer = HTTP send → 2xx per action; the `SLO_ENFORCE=0` grep is deleted; exit = gated smoke **and** one pinned 2,000-agent profile (10% money-path slice, close spike, withdrawals, chaos-healed) as the release artifact; chaos-delay legs excluded from the gated sample set explicitly. |
| grok B6 (fee repricing = live sandwich) | **ACCEPTED.** `fee_bps_override:{market}` is finance+2P, bounds [10,200], Δ≤20bp/5min, delay T; the write **is** a generation bump so `preview_relevant_drift` fires; `MarketFeeChanged` outbox/WS frame in the same tx; `expected_config_version` becomes **required** on PlaceTrade (422 if absent; converse carries it); preview/`FeeContext` read the override. E2E: omitted version 422, mid-live reprice 409s in-flight, stale-token confirm 403. |

## Majors

| Finding | Disposition |
|---|---|
| codex M1 (hold-first protocol) | ACCEPTED — pinned sequence above (B3 row); remote-error policy: request is refused with no hold (idempotent), never silently held. Daily limit: rolling 24h UTC window, reserved by requested+held+sent amounts (grok m2 folded in). |
| codex M2 (webhook/settlement seams) | ACCEPTED — durable inbox contract as B4 row; settlement: lock row, verify stored receipt, ledger leg `withdraw-settle:<id>` + status + event + outbox in one commit. |
| codex M3 (geo/KYC result algebra) | ACCEPTED — US-state **allowlist** (versioned, counsel-owned); `Clear{checked_at,expires_at,policy_version}|Hit|Indeterminate`; only fresh Clear progresses; KYC validity horizon + revocation events; re-screen at send when aged past TTL. |
| codex M4 (dual-control holes) | ACCEPTED — exhaustive money-affecting command matrix added (operation × proposer × confirmer × distinct-token × delay × caps × reason × audits × replay key). Fee override finance+2P (B6 row). AML/sanctions-flagged withdrawals need two-person clearance; below-threshold single-finance approval kept as a **named, capped exception** with boundary tests. Ban/unban full protocol spelled out. |
| codex M5 (one lifetime counter) | ACCEPTED — immutable grant lots + per-lot fee-allocation authority (B1 row); eligible notional = fees actually paid, per unique trade id; unwind reverses attributed progress via compensating fact. |
| codex M6 (reconciliation pages on in-flight) | ACCEPTED — published equation: expected wallet = ledger External ± uniquely-identified broadcast-unfinalized outbound + observed-unbooked finalized inbound suspense; only the residual pages. Opening/treasury facts pinned. Tested across all states. |
| codex M7 + grok M3 (config catalog) | ACCEPTED — full D24 table for every new key (type, closed bounds, safe seed, apply class, role/2P grade, max delta) + cross-key invariants (`min ≤ auto ≤ dual ≤ per_tx_max ≤ daily`, positive depth/multiple/windows, deny-all absent policy); held rows use **current** policy at decision/send; `credit_convert` knobs finance+2P; KYC tiers not writable to `none` except superadmin+2P; `feature_referrals` two-phase and blocked until bind keys exist (grok m3). |
| codex M8 (finality/mint/dest semantics) | ACCEPTED — pinned cluster, mint/decimals, `finalized` commitment for credit+settlement, canonical slot identity, post-credit reorg = accepted house loss stated in ops.md (grok m5), gross-amount semantics with network-fee leg per D8, wrong-mint/dest/amount + late-landing tests. |
| codex M10 + grok M4 (responsible gaming, stranded funds, sweeps) | ACCEPTED — self-exclusion use case (user-initiated, irreversible cooling-off, dual-control lift, blocks grants/trades/deposits/withdraws except return-to-source), user-set deposit limits, banned/sanctioned return-to-source via the compliance send path (ban no longer strands cash by construction), `bonus_structure=real_money|sweeps` config gate so a sweeps ruling doesn't force a rewrite. |
| grok M1 (shadow-limit detectable) | ACCEPTED — homogeneous errors (no ShadowLimited code, no echoed cap), author-visible comments read `visible` to self, no public WS emission, no `status` on self profile; e2e proves indistinguishability from a published tier cap. Shadow users' withdrawals enter `risk_hold` (grok m4). |
| grok M5 (exit criteria lack the attacks) | ACCEPTED — §3 gains red-team legs: wash-after-grant no-convert, A↔B referral once, device/phone third-account zero grant, concurrent withdraw+trade+deposit, dest-cluster block, omitted `expected_config_version` 422, unsigned webhook reject, chain-deposit-without-KYC suspense hold. |
| grok M6 (status propagation ≠ config) | ACCEPTED — `users.status` read under the existing `lock_user` in money mutations; next-request semantics; drain-safe; not a config key. |

## Minors

codex m1 — folded into B1/B5 rows (extend identity 3; keep D27 numbering; per-currency stated). codex m2 — ACCEPTED: durable alert outbox with incident episodes (dedup within open incident, re-page on recurrence, ack/resolve recorded; fake exercises transitions). codex m3 — ACCEPTED: three orthogonal dimensions (money via tx-id constraints; review `screening|review_required|approval_proposed|approved`; send `unsent|sending|broadcast|finalized|definitive_failed|unknown`) mapped onto the ALTERed status machine with a published transition table. grok m1/m2/m3/m4/m5/m6 — all ACCEPTED, folded into the rows above.

**Net effect:** revision 2 rewrites D31–D36, adds D37 (responsible gaming + statuses), the D24 catalog extension table, the invariant/reconciliation supplement, a path-level §2, and red-team exit legs. Round 2 review will target revision 2.
