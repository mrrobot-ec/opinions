# Task 5.4 code review — round 1 resolution map

codex **fix-first** ([codex-p5.4r1.md](codex-p5.4r1.md)): 1 blocker, 2 majors. grok **fix-first** ([grok-p5.4r1.md](grok-p5.4r1.md)): 1 blocker, 2 majors, 3 minors, 1 nit. The blocker converged (independent discovery). Every finding verified against the code before any fix; all fixes TDD (failing test first), suites green, changed files at 100% line coverage.

| Finding | Resolution |
|---|---|
| codex B1 / grok B1+m3 — Stage 2 never reclaims expired `running` leases: a crash after claim-commit (or a tick-fatal error mid-batch) stranded moderation jobs `running` FOREVER — the durable second screen silently gone (fail-open), `moderation_jobs_reclaim_idx` shipped in 0007 but unused | **Implemented the moderation twin of video's reclaim** across the port surface: `VideoTx::reclaim_moderation_expired(now, max_attempts, limit)` (ports/video.rs), pg SQL mirroring video's (SKIP-LOCKED over expired `running` rows in lease-expiry order, clear token/lease, `attempts >= max` → `failed` with `error` coalesced to `lease expired`, else `queued`), fake parity, contract extension in `moderation_jobs_contract` (live-lease untouched; limit honored across two expired rows; re-claim shows next attempt; boundary reclaim terminal — runs against BOTH the fake and pg), and `runner_tick` now reclaims before each claim (`RECLAIM_BATCH = 32`, video worker's bound). Runner tests: crash-after-claim → lease expiry → same tick reclaims/re-claims/re-screens/completes; boundary reclaim is terminal with no further remote call. grok m3's "honest test" demand is exactly these tests. Shared-surface note: the four non-frozen shared files were edited post-wave with pre-edit backups; coordinator notified (msg_26d194ccd7cf). |
| grok M1 — `apply_escalation`/read/CAS failures unwound `run_stage`'s loop, skipping token-fenced completion and orphaning the rest of a claimed batch | `job_work` now isolates ALL per-job work (read, screen, escalate); ANY error it raises is absorbed into the fenced completion (requeue with backoff or terminal at the attempts boundary — parity with video render failures). The loop contains what remains (missing-token invariant, completion-tx failure) per job: the batch tail still completes, the first error fails the tick for observability. Tests: injected comment-tx failure requeues fenced while the tail completes and the tick succeeds; injected completion failure leaves only its own job leased (reclaim's problem), tail completes, tick errs. |
| codex M1 / grok M2 — transport buffered the whole response before any cap; a hostile/broken endpoint could allocate unbounded memory ahead of the 4 KiB/64 KiB checks | `HttpRequest.max_response_bytes` is now part of the transport contract: the real transport rejects an over-cap declared `Content-Length` before reading any body, and otherwise streams chunks, erroring the moment the cap would be crossed — never buffering the tail. Adapters pass their caps (draft 65,536; moderation 4,096) and keep their post-parse checks (defense in depth; the exact-request assertions now also pin the cap each adapter sends). Loopback tests: oversized declared length rejected pre-read; undeclared (close-delimited) stream capped mid-read. |
| codex M2 — header-invalid `LLM_API_KEY` (e.g. embedded newline) passed selection; the local probe then passed startup and every real screen burned attempts to terminal failure | `resolve` now validates the exact header the transport builds (`Bearer {key}` via `HeaderValue::from_str`) at startup; failure is the typed `LlmConfigError::UnencodableApiKey` (message-only — the offending bytes ARE the credential) and poisons both engines, so the probe itself fails and an enabled runner never claims. Tests cover `\n`/`\r`/`\0` keys, no secret in any error text, and the poisoned-probe path. |
| grok m1 — `LlmDraftEngine` freezes `ContentConfig::default()`: env-overridden `DAILY_*`/`FLASH_*` knobs diverge from the template engine's terms (approve refusals or off-policy seeding) | **Escalated, pending frozen-file mediation** (msg_f574b9073b42): the clean fix needs frozen `main.rs:574` to hand the prepared live `ContentConfig` to `draft_engine_from_env`; my selection.rs/draft.rs signature updates are ready to land the moment the coordinator re-manifests. If declined, this stays a documented known limitation (drafts still re-validate against live floors at approve — divergence surfaces loudly as `SeedBelowFloor`, never as silent bad terms reaching a market). |
| grok m2 — `validate_base_url` accepted userinfo; a credential in `LLM_BASE_URL` would leak through transport error strings into `moderation_jobs.error` and logs | Userinfo (username or password) now rejects as a typed `MalformedBaseUrl("userinfo is not allowed")` whose message never echoes the URL. Test asserts the credential text appears in no error rendering. |
| grok N1 — `with_env` stripped both LLM variables from the process instead of restoring the pre-test environment | `with_env` snapshots `var_os` for both names and restores them (set or removed) after the body, poison-safe. Test proves a pre-existing value survives a nested override. |

## Out-of-scope notes carried forward

- grok's probe-landmine note (`AVAILABILITY_PROBE = ""` vs a hypothetical stored empty comment) and the outbox seq-skip note are inherited patterns, unchanged this round.
- `apply_escalation` still emits no `CommentShadowed` event (nothing consumes one today).
- **Pre-existing, not 5.4**: `pg::content_tx::tests::postgres_passes_content_transaction_contract` fails against real Postgres — `contract/content.rs:84` writes a random `published_market` UUID that violates `market_drafts_published_market_id_fkey` (invisible to the fake, which has no FK). 5.1 surface; flagged to the coordinator during the wave and again this round. It blocks the 5.5 barrier's full-crate run.

## Verification

`cargo test -p adapters --lib` 128/129 (the one failure is the pre-existing 5.1 content_tx item above), `cargo test -p application --lib` 335/335 green including 21 moderation_escalate tests and the extended video/moderation contracts against fake + pg; clippy clean; rustfmt clean; 100% line coverage on every file this round touched: transport.rs 330, draft.rs 241, moderation.rs 137, selection.rs 307, moderation_escalate.rs 879, ports/video.rs 8, fakes/video.rs 212, contract/video.rs 874, pg/video_tx.rs 288.
