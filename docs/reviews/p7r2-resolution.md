# Phase 7 review round 2 — coordinator resolutions

Inputs: `codex-p7r2.md` (FIX-FIRST: r1 9 resolved/8 partial/2 unresolved; NEW 6B/9M/3m), `grok-p7r2.md` (fix-first: r1 mint confirmed dead by arithmetic; NEW 2B/4M/4m). Revision 3 carries every accepted resolution. Convergent findings resolved once.

## Convergent blockers

| Finding | Disposition |
|---|---|
| grok M1 + codex NEW-B1 (unwind refunds the fees that bought a convert → 2G extract) | **ACCEPTED — fees are provisional until Paid.** Fee allocations to grant lots finalize only when the fee's market reaches `Paid`; conversion draws exclusively on finalized allocations. Unwind of a Paid market (rare, dual-control) must in the same tx reverse that market's finalized allocations and, if a conversion was funded by them, reverse the convert (`User Usdc → House/BonusReserve`, credits restored) or open a linked receivable if the user cannot cover; converted lots never accept new fees; compensating facts never clear `converted_at`. E2E: grant → convert → void → unwind ⇒ credits restored, reserve restored, user not +2G; BonusWash leg states its exact fee volume so "does not convert" proves the rule, not thin volume. |
| codex NEW-B2 + grok m3 (bonus reserve is just House) | **ACCEPTED.** New `BonusReserve` account class (non-negative, singleton per currency), funded only by dual-control `House(Usdc) → BonusReserve(Usdc)`; only conversion debits it. Coverage invariant: BonusReserve ≥ Σ outstanding **qualified** (finalized-fee) unconverted lot value — transactionally preserved, so an earned conversion can never 422; grant issuance refuses if it would exceed backing. Fees revenue account never pays conversions (D8 reporting stays clean). |
| grok B1 + codex NEW-M1 (suspense refund: no XOR, unbound dest, second authority) | **ACCEPTED.** Canonical deposit authority = **ALTER the existing `deposits` table** (no second observation table): states `observed_finalized → admission_pending → admitted | compliance_hold → refund_approved → refund_sending → refunded`, with `suspense_tx_id`, `admit_tx_id` XOR `refund_tx_id` CHECK, `source_address` captured from chain. Refund is **not** a user withdrawal: dest **equals** the observation's `source_address`, no User credit, no Withheld; settle = `DepositSuspense → External` keyed `deposit-refund:<id>`. Outbound rails generalize to an `outbound_payments` authority with typed subject (`withdrawal | deposit_refund`) sharing the send-attempt machinery and per-subject invariants. Suspense invariant: `balance(DepositSuspense) = Σ` non-terminal observation liabilities. Omnibus-source caveat: refunds to exchange omnibus sources require compliance review (not auto). Row-lock + CAS proven by admit∥refund race test. |
| grok B2 (return-to-source = sanctions/ban hatch) | **ACCEPTED — egress split table.** `banned`/sanctions-`Hit`: NO send; funds freeze in place; only a dual-control counsel-shaped license (dest not user-picked; a Hit source never auto-refunds). Admission-refused + Clear: refund to observation source only (row above). Self-exclusion + Clear: D31 withdrawal to a dest the user has previously **settled** to, or the observation source — full AML/novelty applies; new dest = `risk_hold`. E2E: Hit user + Clear mule dest 403s; Hit source does not auto-refund; self-excluded to new dest holds. |
| codex NEW-B3 (remote-first defeats replay; policy_version in fingerprint) | **ACCEPTED.** Fingerprint = immutable client intent only `(user, amount_micro, canonical dest)`. Order: **persisted hit+fingerprint lookup FIRST** (fast, no locks) → on miss, remote screenings (no locks) → begin → `serialize_key` recheck under guard → locked revalidation → hold. Screening results persist as separate authorization facts (with versions/TTL). Remote refusals persist a replay-stable refusal receipt. Tests: retry-after-provider-outage returns the original receipt; retry-after-policy-bump returns the original receipt (policy applies at decision/send per the tighten-only rule below). |
| codex NEW-B4 (settled rows stop reserving the daily window) | **ACCEPTED.** Reservation = Σ over rows with `requested_at` in the rolling 24h window in ALL states **except** `denied` and definitive-failed. Same rule for per-dest and hot-wallet caps, evaluated under the user/cap lock. Sequential settle-and-retry + concurrent boundary tests. |
| codex NEW-B5 (0011 vs owner-shape constraint; wildcard decoder) | **ACCEPTED.** 0011 explicitly drops/recreates `ledger_accounts_owner_shape` and the singleton unique indexes to admit `withheld`, `deposit_suspense`, `bonus_reserve` (null-owner singletons like house/fees); migration tests for one-per-currency, concurrent get-or-create, non-negativity. Every owner-mapping file (domain, pg trade_tx/rows/invariant_read_tx, fakes/state) enumerated in §2; the invariant reader's unknown→External fallback is **replaced with total decoding that errors on unknown** (never misclassify). |
| codex NEW-B6 (unwind cancel-withdrawals deadlocks vs user-first order) | **ACCEPTED.** Published multi-user unwind algorithm: lock-free participant enumeration → **sorted** user-lock acquisition → market/unwind row lock → re-enumeration; any new participant ⇒ release + retry from enumeration; then cancel/collect unsent withdrawals and compute reversal under the stable lock set. `UnwindTx` += `UserLockGuard`. `ops/unwind_market.rs` + Pg/fake impls get named owners; Pg races proven vs PlaceTrade, withdraw request/send claim, deposit admission, payout. |

## Majors

| Finding | Disposition |
|---|---|
| grok M2 (dest-warming; exit 2 blesses it) | ACCEPTED — dest auto-approve eligibility requires **settled** cumulative ≥ pinned floor AND dest age ≥ T; dust never warms; K=2; refund/RTS dests never enter the warm set; §3.2's "second withdrawal auto-approves" replaced by the floor rule; red-team leg: $1-then-dump stays in review. |
| grok M3 + codex NEW-M5 (n_min unnamed; series composition; timer origin) | ACCEPTED — lib constants: `N_MIN_TRADE` ≥ convoy-2xx count (≥50% trade-capable rule), `N_MIN_CLOSE_TO_PAID` ≥ paid markets in profile incl. one ≥1k-voter book, `N_MIN_WS` ≥ spike tick floor; convoy 2xx INSIDE the gated trade series; `close-to-paid` = authoritative Closed transition → committed Paid event minus only the named risk-hold interval (**never** `tally_hidden_at`), with a deterministic unit trace asserting the computation; chaos-delay legs excluded by name. |
| grok M4 (tighten-only; drift doesn't see override) | ACCEPTED — risk snapshot at request; decision/send applies union(snapshot, current) toward MORE review only (kill switches and compliance always current); `preview_relevant_drift` + `FeeContext.base_bps` must read `fee_bps_override:{market}` (override replaces pool stamp) — both named in §2 with W3 as owner (`ops/config.rs` predicate change via 7.0a signature freeze); e2e: old-generation preview + override change ⇒ 409 even when min_fee/discount unmoved. |
| codex NEW-M2 (non-landing proof; attempt lineage) | ACCEPTED — attempts carry `last_valid_block_height`, monotone attempt number, `replaces_attempt_id`; definitive-failed requires observed finalized height > last-valid AND signature absence from the pinned quorum source; at most one prepared/broadcast/unknown attempt; invariant restated: attempts 1:N, ≤1 finalized attempt, exactly one finalized payment fact per settled withdrawal. Lagging-RPC/fork/two-replacer tests. |
| codex NEW-M3 (dimensions not persisted; legacy columns) | ACCEPTED — explicit `review_state` and `send_state` CHECKed columns + published transition table with impossible-combination constraints; legacy `txn_id` migrated to `hold_tx_id` where derivable else quarantined; legacy `chain_sig` becomes a reference to the finalized attempt; backfill rules per old status stated in 0011. |
| codex NEW-M6 (gas dimensionality) | ACCEPTED — **platform absorbs SOL gas**; settlement delivers and settles the full USDC amount (`Withheld(-A) → External(+A)`); SOL treasury is its own non-USDC operational reconciliation; per-tx min stays ≥ dust floor; confirm echo states full delivery. (D8's "network cost" satisfied by the min + platform absorption; revisit at mainnet.) |
| codex NEW-M7 (aggregate lot counters) | ACCEPTED — append-only `credit_fee_allocations(trade_id, lot_id, amount_micro, kind allocated|finalized|reversed, source_allocation_id, idempotency_key)` with net-per-trade ≤ trade fee and net-per-lot ≤ requirement constraints; deterministic oldest-first split; conversion under lot row lock with namespaced ledger keys + `converted_at` in one tx; replay returns original tx ids; unwind traverses allocation lineage. |
| codex NEW-M8 + grok m2 (OTP is a word) | ACCEPTED — explicit `PhoneVerification` port + sandbox adapter (staging two-factor armed like the faucet) + `phone_verifications` table (normalized-number HMAC, challenge, expiry, attempts, `verified_at`, provider ref, uniqueness); grant issuance requires the durable verification fact; linked-but-unverified channel ⇒ grant 0 (e2e); owner = W2 (port/adapter/table) with W3 consuming. |
| codex NEW-M9 (bonus_structure transitions) | ACCEPTED — each lot stamps an immutable grant class + redemption promise at issuance; a `bonus_structure` change affects only future lots; existing lots honor their stamp; the config proposal preview reports outstanding affected liability and refuses transitions that would strand qualified lots without a funded path (freeze-with-page is the only emergency override, dual-control). Both direction tests. |
| codex NEW-M4 + r1-B6 residual (matrix still bucketed) | ACCEPTED — §2 rewritten one row per exact path incl. `ops/unwind_market.rs`, `resolve_market.rs`, `ops/remedial_credit.rs`, `preview_trade.rs`, http dto/routes files, `services/converse` files, `pg/rows.rs`, `pg/invariant_read_tx.rs`, generated artifacts, tests; pre-wave→wave ownership transfers stated; W4 provides adapters behind 7.0a-declared composition traits and never edits frozen main — coordinator integration barrier reserved for main.rs. |
| codex r1-M7 residual (catalog still prose) | ACCEPTED — revision 3 contains the actual key table (name, JSON type, bounds, seed, apply class, role/2P, max delta) for every Phase 7 key. |
| codex r1-M4 residual (command matrix promised, absent) | ACCEPTED — revision 3 contains the actual money-affecting command matrix (proposer/confirmer/distinct-token/delay/TTL/caps/reason/audits/replay key) incl. ban/unban, manual grant, reserve top-up, flag clear, fee override, refund license. |

## Minors

codex NEW-m1 — ACCEPTED: signed formula `−balance(External) = Σ internal` extended with explicit in-flight terms, attested opening baseline, single as-of cut; numeric example per state. codex NEW-m2 — ACCEPTED: one vocabulary; every SLA/cap query names its persisted dimension. codex NEW-m3 — ACCEPTED: machine admissions write a compliance-decision domain event, never `admin_actions`; manual overrides write both with AdminContext. grok m1 — ACCEPTED: convert-then-collect inside the same `lock_user` tx; progress increments strictly by the Trade txn's `legs.fee` credit (never notional×bps) — stated next to D32. grok m4 — ACCEPTED (folded into NEW-m1/identity list: observation External legs named in identity 4's extension). grok r1-B1 identity residual — phone verification now concrete (NEW-M8 row).

**Net:** revision 3 = D31/D32 rewritten around provisional fees + BonusReserve + deposits-ALTER machine + outbound_payments; D33/D34 egress split; D35 pinned constants; §2 path-exhaustive; the two promised tables (config catalog, command matrix) now exist in the text. Round 3 targets revision 3.
