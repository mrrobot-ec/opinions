1. VERIFIED — `UserLockGuard::lock_user` is explicitly part of `VoteTx`, both stores implement it, `CastVote` orders idempotency/replay → user → market, and two-key advisory classes isolate idempotency locks from user locks with a two-connection contract test.
2. VERIFIED — One worker now owns the entire Rust chain 2.0→2.1→2.2→2.3 sequentially, while only the disjoint `web/` task runs in parallel and 2.5 remains last.
3. REGRESSED — Deadlines and sequence names landed, but `market_snapshot(m)` has no clock/`server_now` input or output for one atomic visibility decision, and the plan does not change `insert_trade` (currently returns only `TradeId`) or add a user lookup to supply source-time `trade_seq`, `created_at`, and `handle`, so the promised enriched `TradePlaced` payload is not implementable as specified.
4. REGRESSED — The curator `UPDATE ... WHERE curator_flagged_at IS NULL RETURNING` correctly elects one event-appending winner, but the current settlement holdings expose only `(AccountId, Side, MicroShares)` and `ResolveTx` has no position-enumeration/update role, so `ResolveMarket` cannot map each payout back to the `(user, outcome)` position that must be zeroed and credited.
5. VERIFIED — The scaffold now uses an exact chosen pnpm 9.x pin, `--yes --disable-git`, and no `--src-dir` flag, while the root probe gets a resolvable pinned `ws` dependency through its own `scripts/package.json`.
6. REGRESSED — Rule 0 names `user_has_channel(u, "imessage")`, but the immediately preceding `VoteReader` contract still declares the old one-argument “any row” method; receipt visibility is also contradictory because `now >= tally_hidden_at` remains true after resolution even though the plan says post-resolution replays reveal `seq`.

ADDENDUM VERDICT: fix-first
