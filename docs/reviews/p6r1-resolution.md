# Phase 6 plan — review round 1 resolution map

Plan revision 2 (`docs/plans/phase6-simswarm-ops.md`) dispositions for codex-p6r1 (10 BLOCKER / 4 MAJOR) and grok-p6r1 (4 BLOCKER / 5 MAJOR / 3 MINOR). Every finding ACCEPTED unless noted; "→" names the plan section carrying the fix.

## codex-p6r1

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | BLOCKER | Outbox seq ≠ commit order; config consumer can skip a pause forever | ACCEPTED → D24: `config_generation` singleton row locked by every write serializes commit order; consumer is a reconciler (outbox = wake-up only) reloading `config_entries` when authoritative generation > applied; two-connection Pg contract test specified. |
| 2 | BLOCKER | Watch-snapshot pause is not a fence; PreviewTrade has no `serialize_key` | ACCEPTED → D25: advisory-class-4 pause fences (global + per-market) acquired shared by PlaceTrade/CastVote in pinned lock-order position, held through commit; pause writes take them exclusive and return after commit. Preview downgraded to best-effort snapshot check + `config_version` stamp (D25a); PlaceTrade authoritative. Race tests enumerated. |
| 3 | MAJOR | Per-key writes → mixed-generation snapshots; one-tunable exit proof | ACCEPTED → D24: `SetConfig` = atomic typed patch over the whole snapshot, cross-key validation, one generation; full key catalog with apply-to classes (live-immutable / next-trade / immediate-global); e2e §3.3 covers bounds, staleness, live-market immutability. |
| 4 | BLOCKER | OpsTx cannot atomically audit existing admin mutations | ACCEPTED → D26: `AuditWrite` role added to the final tx role of EVERY admin mutation (existing AdvanceTx/ResolveTx/CommentTx/ContentTx included) in Task 6.0; before/after captured under the same locked tx; replay/refanout = durable idempotent command + audit in one tx, leased delivery; failure-injection contracts on fake + Pg. |
| 5 | BLOCKER | RBAC incomplete; legacy ADMIN_TOKEN bypass; faucet mint risk | ACCEPTED → D26: exhaustive capability matrix over every /admin/** route, fail-closed middleware, legacy token + fallback removed in 6.0, constant-time digest compare, startup validation, audit-read capability, token hashes redacted; faucet = finance/superadmin + `STAGING_FAUCET=1` arm + per-call cap + startup non-mountability test (§0). |
| 6 | BLOCKER | Unwind states incompatible with lifecycle; clawback breaks non-negativity | ACCEPTED → D30: true unwind legal only from `Voided` or flagged-`Resolving` pre-payout (the payout-hold window); from `Paid` no unwind exists — separate house-funded remedial credit (no clawback, no t0 claim); receivable ledger class for deficits, never negative user rows, never skipped legs. |
| 7 | BLOCKER | Unwind idempotency key insufficient; reversal lineage undefined | ACCEPTED → D30: `market_unwinds(market_id pk, unwind_key unique)` authority; DB-unique `reversed_entry_id` lineage; enumerated treatment of seed/fees/dust/escrow/positions/realizations/projections; concurrency + replay e2e (e). |
| 8 | BLOCKER | Conservation equation wrong vs actual ledger | ACCEPTED → D27: replaced with six exact identities (per-tx sum-zero, non-negativity, external contra identity, payment-fact reconciliation, per-market escrow history with recorded `collateral_at_close`, job idempotency); lifecycle fixtures on fake + Pg. |
| 9 | MAJOR | Invariant queries lack one consistent snapshot | ACCEPTED → D27: `InvariantReadTx` role, Pg = single `REPEATABLE READ, READ ONLY` tx with statement timeout, fake clones state, as-of marker, concurrency contract; swarm calls it only in quiet segments. |
| 10 | BLOCKER | Live replay determinism impossible as claimed | ACCEPTED → D28: determinism narrowed to the planned-intent schedule `(sim_tick, agent_index, local_seq)`; trace recording of market_view + outcomes; `--dry-run` (byte-identical) and `--replay-log` (recorded views) CLI contracts; WS buffered to ticks ordered by source_seq; deterministic sim-time retries; latency in a separate non-compared log; two live runs not claimed identical. |
| 11 | MAJOR | SIGKILL mid-payout is probabilistic, not a proof | ACCEPTED → D29: named armed crash barrier `payout-ledger-written-pre-commit` (staging-armed, readiness ack, kill, restart, exactly-once proof); random kill retained as secondary, not the claim. |
| 12 | MAJOR | Chaos env knobs lack containment; global counter nondeterministic | ACCEPTED → D29: two-factor arm (`OPINIONS_ENV=staging` + `CHAOS_ENABLED=1`), any knob without both = startup error, typed parse-once, /healthz exposes fault set, ordinary profile asserts empty, per-connection deterministic drops, Noop-transparency contract. |
| 13 | BLOCKER | Wave not file-disjoint; 6.0 contracts too weak | ACCEPTED → §2: re-partitioned file-disjoint inventory (W1 config / W2 audit+invariants+unwind in separate route/DTO/pg/use-case files; chaos.rs solely W4); 6.0 pre-writes compiling contracts with behavior-tested Unavailable placeholders (no permissive defaults); wave-stop protocol for frozen changes. |
| 14 | BLOCKER | simswarm fails dependency/HTTP gates; coverage story not credible | ACCEPTED → D28/§2: simswarm = outer harness with zero internal crate deps (own wire DTOs + openapi contract test); transport confined to `simswarm/src/transport.rs` with deps-check extended; `check_dependency_rule.py` ALLOWED updated + frozen in 6.0; 4th coverage recipe `-p simswarm --fail-under-lines 100`; Exec/Sleeper/Transport/Ticker injected roles. |

## grok-p6r1

| # | Sev | Finding | Disposition |
|---|-----|---------|-------------|
| 1 | BLOCKER | Independent personas cannot express coordinated attacks | ACCEPTED → D28: `Ring` abstraction (shared device/IP/channel, one decision fn) + four shipped rings (wash pair, sybil voter cluster, void-suppress, threshold-pad); published scoring.md config seeding; device-id/forwarded-for pinned; ring assertions in e2e §3.1. |
| 2 | BLOCKER | D25 pauses CastVote — oracle kill switch breaking D22 | ACCEPTED → D25: trading pause binds Preview/Place only; CastVote explicitly NOT bound (corridor quotes the D22 contract); separate `voting_paused` = superadmin + reason + public frame + forbidden ≥ tally_hidden_at unless voiding; e2e asserts votes 200 while trades 423. |
| 3 | BLOCKER | Unwind = captured-superadmin refund button; reopens R3 | ACCEPTED → D30: dual control (superadmin proposes, finance confirms ≥T later, two audit rows); legality windows exclude `Paid`; receivables instead of negative/skipped legs; realizations compensating facts; no EWMA rewrite; fees reversed (documented); e2e (a)–(e). |
| 4 | BLOCKER | D24 tunables: no bounds, no live-immutability, no role matrix | ACCEPTED → D24/D26: per-key catalog (bounds tracking published floors, apply-to class, role, max-delta rate) seeded in 0008 + ops.md; live-immutable keys stamped on the market row at go-live; sensitive keys two-phase dual-write; e2e §3.3 covers 422/immutability. |
| 5 | MAJOR | Surprise pause = house-informed dumping | ACCEPTED → D25: public TradingPaused/Resumed frames in the same tx; unpause forbidden inside (tally_hidden_at, closes_at]; reason + role required; ops.md states pause ≠ D22. |
| 6 | MAJOR | 100/5min smoke is theater; nothing closes; replay claim leaks wall time | ACCEPTED → D28/§3: pinned short-market env (FLASH_CLOSES_SECS=180, hidden 60, published defaults, real floors); full lifecycle + one void observed; convoy mode at hidden−15s; SLO report split (hold delay excluded) + SLO_ENFORCE=0 printed + persisted; nightly 2k profile with ≥1k-voter settlement + p95 threshold documented; determinism narrowed + trace replay (with codex #10); invariant sweep scheduled off-spike. |
| 7 | MAJOR | Chaos missing clock/PG/disk/storm/consumer-death | ACCEPTED → D29 scenario list: clock jump, pg_terminate_backend, disk-full render_dir, 100-msg webhook storm, config-reconciler kill + heal bound, simultaneous daily+flash close, plus the original four. |
| 8 | MAJOR | All-Playwright deferral wrong for the mobile window; spec-anchor citation bug | ACCEPTED → §0/§2/§3.8: thin mobile Playwright smoke in W4 + barrier (vote/trade/resolve/share-card, no KYC/card/withdraw); rails stay Phase 7; converse slice not advertised as the rails slice; header re-anchored to spec §§9–10 + §13 item 7 ops half + PLAN.md phase 6. |
| 9 | MAJOR | Young-account posture either disables D21 or voids every market | ACCEPTED → D28 identity policy: 80% aged (staging-only created_at override, armed + audited) / 20% young + ring metadata; integrity profiles never zero the age rule; e2e asserts both the near-close 422 (young) and accepted votes (aged). |
| 10 | MINOR | Preview/place config race half-specified | ACCEPTED → D25a: `config_version` stamped on preview; PlaceTrade 409 StaleConfig on next-trade drift; pause capture via fence-held authoritative read. |
| 11 | MINOR | Faucet is a money door; ban/shadow-limit silently missing | ACCEPTED → §0/D26: faucet finance/superadmin + arm + cap + audit; ban/shadow-limit + deposit/withdraw switches explicitly deferred to Phase 7 rails (stated in §0 Out). |
| 12 | MINOR | ops.md must publish pause ≠ D22 and unwind ≠ void | ACCEPTED → §2/§3 barrier: ops.md per-key table (bounds/class/role/user-visible line) + pause and unwind honesty paragraphs required before 6.5 exits. |

## Cross-review merges

- codex #6/#7 + grok #3 (unwind) merged into one D30: legality windows (codex) + dual control & receivables (grok) + lineage/authority (codex).
- codex #3 + grok #4 (config catalog) merged: atomic snapshot patch + bounds/apply-to/roles/max-delta in one catalog.
- codex #10 + grok #6 (determinism/realism) merged: intent-schedule determinism + trace replay + pinned smoke lifecycle + convoy.
- Both #headers: spec-anchor citation fixed (item 6 = conversation-graph, not this phase).
